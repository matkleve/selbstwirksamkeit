/** Shared dimensions for Zeitspur line + Muster charts (keeps card height stable). */
export const TRAJECTORY_CHART_W = 320
export const TRAJECTORY_CHART_H = 120
export const TRAJECTORY_CHART_ASPECT = `${TRAJECTORY_CHART_W} / ${TRAJECTORY_CHART_H}`

/** Chart SVG + slot labels (line modes) */
export const TRAJECTORY_CHART_BLOCK_MIN_H = '10.75rem'

/** Feld (beide Achsen): larger square, labels inside */
export const TRAJECTORY_FIELD_MAX_SIZE = '12.5rem'
