/** Dark mirror atmosphere — prototype timeline spine lives in MirrorFlow. */
export default function MirrorLayout({ children }: { children: React.ReactNode }) {
  return <div className="mirror-page">{children}</div>
}
